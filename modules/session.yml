name: session
description: >-
  Session middleware to persist user data across requests, a client-side
  useSession composable to interact with the session, supports many storage
  backends via unjs/unstorage
repo: sidebase/nuxt-session
npm: '@sidebase/nuxt-session'
icon: session.png
github: https://github.com/sidebase/nuxt-session
website: https://github.com/sidebase/nuxt-session
learn_more: https://github.com/sidebase
category: Security
type: 3rd-party
maintainers:
  - name: sidebase
    github: sidebase
    twitter: sidebase_io
compatibility:
  nuxt: ^3.0.0
  requires: {}
